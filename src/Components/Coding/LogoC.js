import React from 'react';
import './codingStyle.css';

const LogoC = () => {
    return (
        <section className='logoc' >
            <span>(c)</span>
        </section>
    );
}

export default LogoC;